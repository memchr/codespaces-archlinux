{
	"id": "common",
	"name": "common setup",
	"description": "Install and setup common packages, and setup non-root user.",
	"version": "2.2.0",
	"options": {
		"username": {
			"type": "string",
			"default": "codespace"
		},
		"userUid": {
			"type": "string",
			"default": "1000"
		},
		"userGid": {
			"type": "string",
			"default": "1000"
		}
	}
}